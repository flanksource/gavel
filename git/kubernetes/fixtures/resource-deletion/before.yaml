apiVersion: apps/v1
kind: Deployment
metadata:
  name: old-deployment
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: old
  template:
    metadata:
      labels:
        app: old
    spec:
      containers:
      - name: old
        image: old-app:1.0
