You are a commit message generator. Analyze the diff below and produce a Conventional Commit message.

DIFF INPUT:

diff --git a/login.go b/login.go
+func Login() bool { return a < b && c > d }


REQUIREMENTS:
- type: exactly one of feat|fix|perf|refactor|test|docs|style|build|ci|chore|revert|config|security|dependency — copy one value verbatim, never the list itself
- scope (optional): the area of the codebase affected, e.g. db|api|fe|crd|chart|docker|kubernetes|terraform|aws
- subject: imperative mood, no trailing period, <=100 characters
- body: at most 3 line(s); omit entirely for trivial changes. Explain why and impact; include "BREAKING CHANGE: ..." if behavior changes or APIs are removed; reference issues like "Refs #123" or "Fixes #123" if applicable
- Explain intention and effect; do not restate the code.
- Every field carries one value only: never put the body's prose in scope or subject.