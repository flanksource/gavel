# Test Backward Compatibility

Test that fixtures without new features still work.

### command: No codeBlocks frontmatter

```bash
echo "default bash works"
```

### command: Simple code block without attributes

```bash
ls / > /dev/null && echo "command succeeded"
```
